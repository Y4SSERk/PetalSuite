<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.*?>

<BorderPane xmlns="http://javafx.com/javafx/17" xmlns:fx="http://javafx.com/fxml/1" 
            fx:controller="com.florist.controller.SupplierManagementController"
            prefHeight="600.0" prefWidth="900.0" styleClass="root">
    
    <!-- Top - Header -->
    <top>
        <VBox styleClass="header">
            <padding>
                <Insets top="15" right="20" bottom="15" left="20"/>
            </padding>
            <Label text="Gestion des Fournisseurs" styleClass="title">
                <font><Font name="System Bold" size="24"/></font>
            </Label>
        </VBox>
    </top>
    
    <!-- Center - Table and Form -->
    <center>
        <SplitPane dividerPositions="0.6">
            <!-- Left - Table -->
            <VBox spacing="10">
                <padding>
                    <Insets top="10" right="10" bottom="10" left="10"/>
                </padding>
                
                <HBox spacing="10" alignment="CENTER_LEFT">
                    <Button fx:id="refreshBtn" text="🔄 Actualiser" onAction="#handleRefresh"/>
                    <Button fx:id="deleteBtn" text="🗑️ Supprimer" onAction="#handleDelete" styleClass="danger-button"/>
                </HBox>
                
                <TableView fx:id="supplierTable" VBox.vgrow="ALWAYS">
                    <columns>
                        <TableColumn fx:id="idColumn" text="ID" prefWidth="60"/>
                        <TableColumn fx:id="nameColumn" text="Nom" prefWidth="200"/>
                        <TableColumn fx:id="phoneColumn" text="Téléphone" prefWidth="150"/>
                        <TableColumn fx:id="emailColumn" text="Email" prefWidth="200"/>
                    </columns>
                </TableView>
            </VBox>
            
            <!-- Right - Form -->
            <VBox spacing="15" styleClass="form-panel">
                <padding>
                    <Insets top="15" right="15" bottom="15" left="15"/>
                </padding>
                
                <Label text="Détails du Fournisseur" styleClass="form-title">
                    <font><Font name="System Bold" size="16"/></font>
                </Label>
                
                <GridPane hgap="10" vgap="15">
                    <Label text="Nom:" GridPane.rowIndex="0" GridPane.columnIndex="0"/>
                    <TextField fx:id="nameField" promptText="Nom du fournisseur" 
                               GridPane.rowIndex="0" GridPane.columnIndex="1" prefWidth="250"/>
                    
                    <Label text="Téléphone:" GridPane.rowIndex="1" GridPane.columnIndex="0"/>
                    <TextField fx:id="phoneField" promptText="01-23-45-67-89" 
                               GridPane.rowIndex="1" GridPane.columnIndex="1"/>
                    
                    <Label text="Email:" GridPane.rowIndex="2" GridPane.columnIndex="0"/>
                    <TextField fx:id="emailField" promptText="contact@example.com" 
                               GridPane.rowIndex="2" GridPane.columnIndex="1"/>
                </GridPane>
                
                <HBox spacing="10" alignment="CENTER">
                    <Button fx:id="addBtn" text="➕ Ajouter" onAction="#handleAdd" styleClass="success-button"/>
                    <Button fx:id="updateBtn" text="✏️ Modifier" onAction="#handleUpdate" styleClass="primary-button"/>
                    <Button fx:id="clearBtn" text="🔄 Effacer" onAction="#handleClear"/>
                </HBox>
            </VBox>
        </SplitPane>
    </center>
    
    <!-- Bottom - Back Button -->
    <bottom>
        <HBox spacing="10" alignment="CENTER_LEFT" styleClass="bottom-bar">
            <padding>
                <Insets top="10" right="20" bottom="10" left="20"/>
            </padding>
            <Button fx:id="backBtn" text="← Retour au Menu" onAction="#handleBack"/>
        </HBox>
    </bottom>
</BorderPane>
